{{ define "model/additional/interfaces" }}
  {{- if eq $.Name "Tenant" }}
    // IsMetadataNode implements interface for MetadataNode
    func ({{ $.Receiver }} {{ $.Name }}) IsMetadataNode() {}

    // IsResourceContainer implements interface for ResourceContainer
    func ({{ $.Receiver }} {{ $.Name }}) IsResourceContainer() {}
  {{ end }}
{{ end }}

{{ define "interfaces" }}
{{ $pkg := base $.Config.Package }}
{{ template "header" $ }}

type MetadataNode interface {
	IsMetadataNode()
  IsEntity()
}

type ResourceContainer interface {
	IsResourceContainer()
  IsEntity()
}
{{ end }}
